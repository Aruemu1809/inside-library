<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>ログイン - <%= type === 'core' ? '初期メン' : '撮影班' %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="login-body">
  <div class="login-container animate-fade-in">
    <h2><%= type === 'core' ? '🌟 初期メン用ログイン 🌟' : '🎥 撮影班ログイン 🎥' %></h2>
    <form action="/login" method="post">
      <input name="id" placeholder="ユーザーID" required>
      <input name="password" type="password" placeholder="パスワード" required>
      <button type="submit">ログイン</button>
    </form>

    <div class="switch-links">
      <% if (type === 'core') { %>
        <p>🎥 撮影班のかたは <a href="/login?type=yt">こちら</a></p>
      <% } else { %>
        <p>🌟 初期メンバーのかたは <a href="/login?type=core">こちら</a></p>
      <% } %>
    </div>
  </div>
</body>
</html>
